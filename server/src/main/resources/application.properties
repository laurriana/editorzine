# on the cloud
#spring.datasource.url=${CLOUD_URL}
#spring.datasource.username=${CLOUD_USERNAME}
#spring.datasource.password=${CLOUD_PASSWORD}

# when the proxy isn't working
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/editorzine
spring.datasource.username=${DS_USERNAME}
spring.datasource.password=${DS_PASSWORD}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-ms=3600000